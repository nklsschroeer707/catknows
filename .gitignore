# Browser login profile — holds your Skool session, NEVER commit
.skool-profile/

# Output vaults
vault/
*.vault/

# Local MCP registration (holds machine-specific absolute paths)
.mcp.json

# Workspace runs are personal: results stay local, only the structure ships
workspaces/**/output/
workspaces/_config/me.md
workspaces/_config/mobile-mcp-private.md

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/

# OS
.DS_Store
Thumbs.db
